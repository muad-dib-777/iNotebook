import React from 'react'


export const About = () => {

  
  
  return (
    <div>
      This iNotebook app was built by Deepro Nath, a 3rd year student studying in IIT BHU. The purpose of the app is to allow users to store their notes on the cloud. For the techies out there, the frontend was built using React, styled using Bootstrap, and the backend API was built with Node and Express, integrating with MongoDB to handle CRUD operations. Authentication has also been added using bcryptjs and password hashing to enable sign-up and log-in functionalities.
    </div>
  )
}
